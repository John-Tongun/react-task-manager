export default function SearchBar() {
    return (
        <div>
        <input
           type="text"
             placeholder="Search tasks..."
            <button>Search</button>
        </div>
    );
}